root = "/app"
tmp_dir = "tmp"
[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "tmp/server"
  include_ext = ["go"]
  exclude_dir = ["vendor", "tmp"]
  log = "stdout"
[log]
  time = true
